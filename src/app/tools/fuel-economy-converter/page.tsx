"use client";
import { useState } from "react";
export default function Page() {
  const [val, setVal] = useState("");
  const [from, setFrom] = useState("mpg_us");
  const [to, setTo] = useState("mpg_uk");
  const factors: Record<string,number> = {mpg_us: 1, mpg_uk: 1, l100km: 1, kml: 1, km_gal_us: 1};
  const result = val && factors[from] && factors[to] ? ((parseFloat(val) * factors[from]) / factors[to]).toFixed(6) : "";
  return (<div style={{padding:"2rem",maxWidth:"600px",margin:"0 auto",fontFamily:"sans-serif"}}><h1 style={{fontSize:"1.8rem",marginBottom:"0.5rem"}}>Fuel Economy Converter</h1><p style={{color:"#888",marginBottom:"1.5rem"}}>Convert between MPG, L/100km, and km/L fuel efficiency units.</p><div style={{display:"flex",gap:"1rem",flexWrap:"wrap",alignItems:"center"}}><input type="number" value={val} onChange={e=>setVal(e.target.value)} placeholder="Value" style={{padding:"0.5rem",fontSize:"1rem",borderRadius:"6px",border:"1px solid #444",background:"#1a1a1a",color:"#fff",width:"140px"}}/><select value={from} onChange={e=>setFrom(e.target.value)} style={{padding:"0.5rem",fontSize:"1rem",borderRadius:"6px",border:"1px solid #444",background:"#1a1a1a",color:"#fff"}}><option value="mpg_us">mpg (US)</option><option value="mpg_uk">mpg (UK)</option><option value="l100km">L/100km</option><option value="kml">km/L</option><option value="km_gal_us">km/gallon (US)</option></select><span>→</span><select value={to} onChange={e=>setTo(e.target.value)} style={{padding:"0.5rem",fontSize:"1rem",borderRadius:"6px",border:"1px solid #444",background:"#1a1a1a",color:"#fff"}}><option value="mpg_us">mpg (US)</option><option value="mpg_uk">mpg (UK)</option><option value="l100km">L/100km</option><option value="kml">km/L</option><option value="km_gal_us">km/gallon (US)</option></select></div>{result && <div style={{marginTop:"1.5rem",padding:"1rem",background:"#1a1a1a",borderRadius:"8px",fontSize:"1.5rem",color:"#4ade80"}}><strong>{val} {from}</strong> = <strong>{result} {to}</strong></div>}</div>);
}
