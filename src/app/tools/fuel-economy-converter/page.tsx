'use client';import { useState } from 'react';export default function FuelEconomyConverter() { const [val, setVal] = useState(''); const [from, setFrom] = useState('mpg_us'); const [to, setTo] = useState('lper100km'); const toBase = { mpg_us: v => 235.214 / v, mpg_uk: v => 282.481 / v, lper100km: v => v, kmpL: v => 100 / v }; const fromBase = { mpg_us: v => 235.214 / v, mpg_uk: v => 282.481 / v, lper100km: v => v, kmpL: v => 100 / v }; const labels = { mpg_us:'MPG (US)', mpg_uk:'MPG (UK)', lper100km:'L/100km', kmpL:'km/L' }; const convert = () => { const v = parseFloat(val); if (isNaN(v) || v === 0) return ''; try { return fromBase[to](toBase[from](v)).toFixed(4); } catch { return ''; } }; return (<div style={{padding:'2rem',fontFamily:'monospace',background:'#0f172a',minHeight:'100vh',color:'#e2e8f0'}}><h1 style={{color:'#38bdf8'}}>Fuel Economy Converter</h1><input value={val} onChange={e=>setVal(e.target.value)} placeholder='Value' style={{padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginRight:'0.5rem'}} /><select value={from} onChange={e=>setFrom(e.target.value)} style={{padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginRight:'0.5rem'}}>{Object.keys(labels).map(u=><option key={u} value={u}>{labels[u]}</option>)}</select><span style={{color:'#94a3b8'}}>to</span><select value={to} onChange={e=>setTo(e.target.value)} style={{padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',margin:'0 0.5rem'}}>{Object.keys(labels).map(u=><option key={u} value={u}>{labels[u]}</option>)}</select><div style={{marginTop:'1rem',fontSize:'1.5rem',color:'#4ade80'}}>{convert()} {labels[to]}</div></div>); }