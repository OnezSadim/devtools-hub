export const metadata = {
  title: 'ROI Calculator | DevTools Hub',
  description: 'Calculate return on investment and annualized ROI for any investment.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
