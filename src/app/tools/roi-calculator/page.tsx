'use client';import{useState}from 'react';export default function P(){const[inv,setInv]=useState('1000');const[ret,setRet]=useState('1500');const roi=(parseFloat(ret)-parseFloat(inv))/parseFloat(inv)*100;return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>ROI Calculator</h1><p>Initial Investment ($):<input value={inv} onChange={e=>setInv(e.target.value)} type='number'/></p><p>Final Value ($):<input value={ret} onChange={e=>setRet(e.target.value)} type='number'/></p><p><b>ROI: {isNaN(roi)?'0.00':roi.toFixed(2)}%</b></p><p>Net Profit: ${isNaN(roi)?'0.00':(parseFloat(ret)-parseFloat(inv)).toFixed(2)}</p><p>{roi>=0?'Positive return':'Negative return'}</p></div>);}