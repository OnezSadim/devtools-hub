import type { Metadata } from "next";
export const metadata: Metadata = { title: "Line Sorter | DevTools Hub", description: "Sort lines alphabetically, reverse, or by length. Remove duplicate lines from text." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }