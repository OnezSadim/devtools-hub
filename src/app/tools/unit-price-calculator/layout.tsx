export const metadata = {
  title: 'Unit Price Calculator | DevTools Hub',
  description: 'Compare unit prices to find the best value when shopping.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
