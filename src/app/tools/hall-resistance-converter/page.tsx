"use client";import{useState}from"react";const U=[{"label":"Ω","factor":1.0},{"label":"kΩ","factor":1000.0},{"label":"RK (von Klitzing)","factor":25812.807},{"label":"mΩ","factor":0.001},{"label":"MΩ","factor":1000000.0}];export default function P(){const[v,setV]=useState("");const[f,setF]=useState(0);const[t,setT]=useState(1);const c=()=>{const n=parseFloat(v);if(isNaN(n))return"";return((n*U[f].factor)/U[t].factor).toPrecision(6);};return(<div style={{padding:"2rem",fontFamily:"sans-serif",maxWidth:"600px",margin:"0 auto"}}><h1>Hall Resistance Converter</h1><p>Convert Hall resistance and von Klitzing constant units</p><input type="number" value={v} onChange={e=>setV(e.target.value)} placeholder="Value" style={{width:"100%",padding:"0.5rem",marginBottom:"1rem"}}/><div style={{display:"flex",gap:"1rem",marginBottom:"1rem"}}><select value={f} onChange={e=>setF(Number(e.target.value))} style={{flex:1,padding:"0.5rem"}}>{U.map((u,i)=>(<option key={i} value={i}>{u.label}</option>))}</select><span style={{alignSelf:"center"}}>→</span><select value={t} onChange={e=>setT(Number(e.target.value))} style={{flex:1,padding:"0.5rem"}}>{U.map((u,i)=>(<option key={i} value={i}>{u.label}</option>))}</select></div>{v&&<div style={{padding:"1rem",background:"#f0f0f0",borderRadius:"4px"}}><strong>{c()}</strong> {U[t].label}</div>}</div>);}