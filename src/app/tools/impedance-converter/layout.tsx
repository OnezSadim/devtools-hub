---
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Impedance Converter | DevTools Hub", description: "Convert between electrical impedance units." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }