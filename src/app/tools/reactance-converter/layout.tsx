---
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Reactance Converter | DevTools Hub", description: "Convert between electrical reactance units (inductive and capacitive)." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }