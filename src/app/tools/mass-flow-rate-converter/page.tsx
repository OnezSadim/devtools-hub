"use client";import{useState}from"react";export default function Page(){const[val,setVal]=useState("");const[from,setFrom]=useState(""+"kilogram_per_second"+"");const[to,setTo]=useState(""+"kilogram_per_hour"+"");function convert(){if(!val||isNaN(Number(val)))return"Invalid";let v=parseFloat(val),base=0;switch(from){case "kilogram_per_second": base = v / 1; break;case "kilogram_per_hour": base = v / 0.000277778; break;case "pound_per_second": base = v / 0.453592; break;case "pound_per_hour": base = v / 0.000125998; break;case "ton_per_hour": base = v / 0.277778; break;case "gram_per_second": base = v / 0.001; break;}let result=0;switch(to){case "kilogram_per_second": result = base * 1; break;case "kilogram_per_hour": result = base * 0.000277778; break;case "pound_per_second": result = base * 0.453592; break;case "pound_per_hour": result = base * 0.000125998; break;case "ton_per_hour": result = base * 0.277778; break;case "gram_per_second": result = base * 0.001; break;}return result.toPrecision(6);}return(<div style={{padding:"2rem",fontFamily:"sans-serif",maxWidth:600,margin:"0 auto"}}><h1>Mass Flow Rate Converter</h1><p>Convert between kilograms per second, pounds per hour, tons per day, and more</p><input type="number" value={val} onChange={e=>setVal(e.target.value)} placeholder="Value" style={{padding:"0.5rem",marginRight:"0.5rem",width:120}}/><select value={from} onChange={e=>setFrom(e.target.value)} style={{padding:"0.5rem",marginRight:"0.5rem"}}><option value="kilogram_per_second">kilogram per second</option><option value="kilogram_per_hour">kilogram per hour</option><option value="pound_per_second">pound per second</option><option value="pound_per_hour">pound per hour</option><option value="ton_per_hour">ton per hour</option><option value="gram_per_second">gram per second</option></select><span>→</span><select value={to} onChange={e=>setTo(e.target.value)} style={{padding:"0.5rem",marginLeft:"0.5rem"}}><option value="kilogram_per_second">kilogram per second</option><option value="kilogram_per_hour">kilogram per hour</option><option value="pound_per_second">pound per second</option><option value="pound_per_hour">pound per hour</option><option value="ton_per_hour">ton per hour</option><option value="gram_per_second">gram per second</option></select><div style={{marginTop:"1rem",fontSize:"1.5rem"}}>{val&&<span>{val} {from.replace(/_/g," ")} = <strong>{convert()}</strong> {to.replace(/_/g," ")}</span>}</div></div>);}