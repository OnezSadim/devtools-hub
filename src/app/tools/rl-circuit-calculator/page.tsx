'use client';import{useState}from 'react';export default function T(){const[r,setR]=useState('');const[l,setL]=useState('');const[res,setRes]=useState('');function calc(){const R=parseFloat(r),L=parseFloat(l)*1e-3;if(!R||!L)return;const tau=L/R;const fc=R/(2*Math.PI*L);setRes('tau='+tau.toExponential(3)+'s | fc='+fc.toFixed(2)+'Hz');}return(<div style={{padding:'2rem',fontFamily:'monospace',maxWidth:'600px',margin:'0 auto'}}><h1>RL Circuit Calculator</h1><p>Time constant and cutoff frequency for RL circuits.</p><div><label>R (Ohm): <input type='number' value={r} onChange={e=>setR(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><div><label>L (mH): <input type='number' value={l} onChange={e=>setL(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><button onClick={calc} style={{padding:'0.5rem 1rem',margin:'0.5rem 0',cursor:'pointer'}}>Calculate</button>{res&&<pre style={{background:'#f5f5f5',padding:'1rem'}}>{res}</pre>}</div>);}