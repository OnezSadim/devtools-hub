'use client';import{useState}from 'react';export default function Page(){const[fmax,setFmax]=useState('1000');const[r,setR]=useState('');function calc(){const f=parseFloat(fmax);if(isNaN(f)){setR('Enter a valid frequency.');return;}const nyq=2*f;const rec=2.5*f;setR('Minimum (Nyquist): '+(nyq).toLocaleString()+' Hz | Recommended: '+(rec).toLocaleString()+' Hz');}return(<div style={{padding:'2rem',fontFamily:'monospace',background:'#0a0a0a',minHeight:'100vh',color:'#e0e0e0'}}><h1 style={{color:'#7c3aed'}}>Sampling Theorem Calculator</h1><p>Find the minimum sampling rate needed to reconstruct a signal (Shannon-Nyquist theorem).</p><label>Maximum Signal Frequency (Hz):</label><input value={fmax} onChange={e=>setFmax(e.target.value)} style={{display:'block',margin:'8px 0',padding:'8px',background:'#1a1a2e',border:'1px solid #333',color:'#e0e0e0',width:'220px'}}/><button onClick={calc} style={{background:'#7c3aed',color:'white',border:'none',padding:'10px 20px',cursor:'pointer'}}>Calculate</button>{r&&<p style={{marginTop:'1rem',color:'#4ade80'}}>{r}</p>}<p style={{marginTop:'1rem',color:'#888'}}>The sampling rate must be at least 2x the highest signal frequency to avoid aliasing.</p></div>);}