'use client';import{useState}from 'react';export default function MortgageCalculator(){const[home,setHome]=useState('300000');const[down,setDown]=useState('60000');const[rate,setRate]=useState('6.5');const[years,setYears]=useState('30');const[result,setResult]=useState(null);function calc(){const p=parseFloat(home)-parseFloat(down);const r=parseFloat(rate)/100/12;const n=parseFloat(years)*12;const m=p*(r*Math.pow(1+r,n))/(Math.pow(1+r,n)-1);const total=m*n;setResult({monthly:m.toFixed(2),total:total.toFixed(2),interest:(total-p).toFixed(2)});}return(<div style={{fontFamily:'monospace',background:'#0f172a',minHeight:'100vh',padding:'2rem',color:'#e2e8f0'}}><h1 style={{fontSize:'1.5rem',marginBottom:'1rem'}}>Mortgage Calculator</h1><div style={{display:'grid',gap:'1rem',maxWidth:'400px'}}><div><label>Home Price ($)</label><input value={home} onChange={e=>setHome(e.target.value)} style={{width:'100%',padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginTop:'0.25rem'}} /></div><div><label>Down Payment ($)</label><input value={down} onChange={e=>setDown(e.target.value)} style={{width:'100%',padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginTop:'0.25rem'}} /></div><div><label>Interest Rate (%)</label><input value={rate} onChange={e=>setRate(e.target.value)} style={{width:'100%',padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginTop:'0.25rem'}} /></div><div><label>Loan Term (years)</label><input value={years} onChange={e=>setYears(e.target.value)} style={{width:'100%',padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginTop:'0.25rem'}} /></div><button onClick={calc} style={{padding:'0.75rem',background:'#3b82f6',color:'white',border:'none',borderRadius:'4px',cursor:'pointer'}}>Calculate</button>{result&&<div style={{background:'#1e293b',padding:'1rem',borderRadius:'4px'}}><p>Monthly Payment: <strong>${result.monthly}</strong></p><p>Total Payment: <strong>${result.total}</strong></p><p>Total Interest: <strong>${result.interest}</strong></p></div>}</div></div>);}
