"use client";import{useState}from"react";const U=[{"label":"Wb","factor":1.0},{"label":"Φ₀ (flux quantum)","factor":2.067834e-15},{"label":"mWb","factor":0.001},{"label":"μWb","factor":1e-06},{"label":"nWb","factor":1e-09}];export default function P(){const[v,setV]=useState("");const[f,setF]=useState(0);const[t,setT]=useState(1);const c=()=>{const n=parseFloat(v);if(isNaN(n))return"";return((n*U[f].factor)/U[t].factor).toPrecision(6);};return(<div style={{padding:"2rem",fontFamily:"sans-serif",maxWidth:"600px",margin:"0 auto"}}><h1>Magnetic Flux Quantum Converter</h1><p>Convert magnetic flux in units of flux quantum</p><input type="number" value={v} onChange={e=>setV(e.target.value)} placeholder="Value" style={{width:"100%",padding:"0.5rem",marginBottom:"1rem"}}/><div style={{display:"flex",gap:"1rem",marginBottom:"1rem"}}><select value={f} onChange={e=>setF(Number(e.target.value))} style={{flex:1,padding:"0.5rem"}}>{U.map((u,i)=>(<option key={i} value={i}>{u.label}</option>))}</select><span style={{alignSelf:"center"}}>→</span><select value={t} onChange={e=>setT(Number(e.target.value))} style={{flex:1,padding:"0.5rem"}}>{U.map((u,i)=>(<option key={i} value={i}>{u.label}</option>))}</select></div>{v&&<div style={{padding:"1rem",background:"#f0f0f0",borderRadius:"4px"}}><strong>{c()}</strong> {U[t].label}</div>}</div>);}