'use client';import{useState}from 'react';export default function P(){const[fc,setFc]=useState('10000');const[vc,setVc]=useState('50');const[pr,setPr]=useState('100');const units=parseFloat(fc)/(parseFloat(pr)-parseFloat(vc));const rev=Math.ceil(units)*parseFloat(pr);return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>Break-Even Calculator</h1><p>Fixed Costs ($):<input value={fc} onChange={e=>setFc(e.target.value)} type='number'/></p><p>Variable Cost/Unit ($):<input value={vc} onChange={e=>setVc(e.target.value)} type='number'/></p><p>Price/Unit ($):<input value={pr} onChange={e=>setPr(e.target.value)} type='number'/></p><p><b>Break-Even Units: {isNaN(units)||!isFinite(units)?'N/A':Math.ceil(units)}</b></p><p>Break-Even Revenue: ${isNaN(rev)||!isFinite(rev)?'N/A':rev.toFixed(2)}</p></div>);}