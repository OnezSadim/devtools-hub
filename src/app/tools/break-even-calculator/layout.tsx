export const metadata = {
  title: 'Break-Even Calculator | DevTools Hub',
  description: 'Calculate break-even point for your business or product.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
