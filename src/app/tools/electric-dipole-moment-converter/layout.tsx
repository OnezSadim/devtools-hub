---
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Electric Dipole Moment Converter | DevTools Hub", description: "Convert between electric dipole moment units." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }