---
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Conductance Converter | DevTools Hub", description: "Convert between electrical conductance units including siemens and mho." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }