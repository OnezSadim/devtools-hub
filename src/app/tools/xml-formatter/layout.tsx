import type { Metadata } from "next";
export const metadata: Metadata = { title: "XML Formatter | DevTools Hub", description: "Format and prettify XML code online. Validate and indent XML with syntax highlighting." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }