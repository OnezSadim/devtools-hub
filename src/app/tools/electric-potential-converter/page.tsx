"use client";import React,{useState} from "react";const units={{"V":1,"mV":0.001,"uV":1e-06,"kV":1000,"MV":1000000,"GV":1000000000}};export default function Page(){const[val,setVal]=useState("1");const[from,setFrom]=useState(Object.keys(units)[0]);const num=parseFloat(val)||0;const base=num*units[from];return(<div style={{fontFamily:"sans-serif",maxWidth:480,margin:"40px auto",padding:16}}><h1>Electric Potential Converter</h1><p>Convert between volts, millivolts, microvolts, kilovolts, megavolts</p><input type="number" value={val} onChange={e=>setVal(e.target.value)} style={{width:"100%",padding:8,marginBottom:8,boxSizing:"border-box"}}/><select value={from} onChange={e=>setFrom(e.target.value)} style={{width:"100%",padding:8,marginBottom:16}}>{Object.keys(units).map(u=>(<option key={u} value={u}>{u}</option>))}</select><table style={{width:"100%",borderCollapse:"collapse"}}><tbody>{Object.entries(units).map(([u,r])=>(<tr key={u} style={{borderBottom:"1px solid #eee"}}><td style={{padding:"6px 8px",fontWeight:"bold"}}>{u}</td><td style={{padding:"6px 8px",textAlign:"right"}}>{(base/r).toPrecision(6)}</td></tr>))}</tbody></table></div>);}
