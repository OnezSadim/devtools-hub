---
import type { Metadata } from "next";
export const metadata: Metadata = { title: "Electric Potential Converter | DevTools Hub", description: "Convert between electric potential and voltage units." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }