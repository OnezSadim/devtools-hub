'use client';import{useState}from 'react';export default function Page(){const[d,setD]=useState('');const[v,setV]=useState('');const[res,setRes]=useState('');const calc=()=>{const D=parseFloat(d),V=parseFloat(v);if(!D||!V){setRes('Enter values');return;}const A=Math.PI*(D/2)**2;const Q=A*V;setRes('Flow rate: '+Q.toFixed(4)+' m3/s, Area: '+A.toFixed(4)+' m2');};return(<div style={{padding:'20px',fontFamily:'monospace'}}><h1>Pipe Flow Calculator</h1><label>Diameter (m): <input value={d} onChange={e=>setD(e.target.value)} style={{margin:'5px'}}/></label><br/><label>Velocity (m/s): <input value={v} onChange={e=>setV(e.target.value)} style={{margin:'5px'}}/></label><br/><button onClick={calc} style={{margin:'10px',padding:'8px 16px'}}>Calculate</button>{res&&<div style={{marginTop:'10px',padding:'10px',background:'#f0f0f0'}}>{res}</div>}</div>);}