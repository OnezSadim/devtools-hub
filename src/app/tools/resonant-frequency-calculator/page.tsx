'use client';import{useState}from 'react';export default function T(){const[l,setL]=useState('');const[c,setC]=useState('');const[res,setRes]=useState('');function calc(){const L=parseFloat(l)*1e-3,C=parseFloat(c)*1e-6;if(!L||!C)return;const f=1/(2*Math.PI*Math.sqrt(L*C));const w=2*Math.PI*f;setRes('f='+f.toFixed(4)+'Hz | w='+w.toFixed(4)+'rad/s | lambda='+(3e8/f).toExponential(3)+'m');}return(<div style={{padding:'2rem',fontFamily:'monospace',maxWidth:'600px',margin:'0 auto'}}><h1>Resonant Frequency Calculator</h1><p>Calculate LC circuit resonant frequency.</p><div><label>L (mH): <input type='number' value={l} onChange={e=>setL(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><div><label>C (uF): <input type='number' value={c} onChange={e=>setC(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><button onClick={calc} style={{padding:'0.5rem 1rem',margin:'0.5rem 0',cursor:'pointer'}}>Calculate</button>{res&&<pre style={{background:'#f5f5f5',padding:'1rem'}}>{res}</pre>}</div>);}