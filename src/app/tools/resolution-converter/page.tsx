'use client';import { useState } from 'react';export default function ResolutionConverter() { const [w, setW] = useState(''); const [h, setH] = useState(''); const [result, setResult] = useState(null); const PRESETS = [{name:'HD 720p',w:1280,h:720},{name:'Full HD 1080p',w:1920,h:1080},{name:'2K QHD',w:2560,h:1440},{name:'4K UHD',w:3840,h:2160},{name:'8K',w:7680,h:4320},{name:'SD 480p',w:854,h:480},{name:'Instagram Square',w:1080,h:1080},{name:'Instagram Story',w:1080,h:1920}]; const calc = () => { const width = parseFloat(w), height = parseFloat(h); if (!width || !height) return; setResult({ mp: (width * height / 1e6).toFixed(2), ar: (width / height).toFixed(4), closest: PRESETS.sort((a,b) => Math.abs(a.w - width) - Math.abs(b.w - width))[0].name }); }; return (<div style={{padding:'2rem',maxWidth:'600px',margin:'0 auto',fontFamily:'monospace'}}><h1 style={{color:'#a78bfa'}}>Resolution Converter</h1><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:'0.5rem',marginBottom:'0.75rem'}}>{PRESETS.map(p => (<button key={p.name} onClick={() => { setW(String(p.w)); setH(String(p.h)); }} style={{padding:'0.5rem',background:'#1e1b4b',color:'#94a3b8',border:'1px solid #4c1d95',borderRadius:'6px',cursor:'pointer',textAlign:'left',fontSize:'0.8rem'}}>{p.name}: {p.w}x{p.h}</button>))}</div><div style={{display:'flex',gap:'1rem',marginBottom:'0.75rem'}}><input value={w} onChange={e => setW(e.target.value)} type='number' placeholder='Width px' style={{flex:1,padding:'0.75rem',background:'#1e1b4b',color:'#e2e8f0',border:'1px solid #4c1d95',borderRadius:'6px'}} /><input value={h} onChange={e => setH(e.target.value)} type='number' placeholder='Height px' style={{flex:1,padding:'0.75rem',background:'#1e1b4b',color:'#e2e8f0',border:'1px solid #4c1d95',borderRadius:'6px'}} /></div><button onClick={calc} style={{width:'100%',padding:'0.75rem',background:'#7c3aed',color:'#fff',border:'none',borderRadius:'6px',cursor:'pointer',marginBottom:'1rem'}}>Analyze</button>{result && <div style={{background:'#1e1b4b',borderRadius:'6px',padding:'1rem'}}><p style={{color:'#a78bfa',margin:'0.25rem 0'}}>Megapixels: {result.mp} MP</p><p style={{color:'#94a3b8',margin:'0.25rem 0'}}>Aspect ratio: {result.ar}</p><p style={{color:'#94a3b8',margin:'0.25rem 0'}}>Closest preset: {result.closest}</p></div>}</div>); }