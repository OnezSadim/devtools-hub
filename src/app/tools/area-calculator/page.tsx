'use client';import{useState}from 'react';export default function Page(){const[shape,setShape]=useState('rectangle');const[a,setA]=useState('');const[b,setB]=useState('');const[result,setResult]=useState('');const calc=()=>{const x=parseFloat(a),y=parseFloat(b);let area=0;if(shape==='rectangle')area=x*y;else if(shape==='triangle')area=0.5*x*y;else if(shape==='circle')area=Math.PI*x*x;else if(shape==='trapezoid')area=0.5*(x+y)*parseFloat(b);setResult(area.toFixed(4))};return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>Area Calculator</h1><div style={{display:'flex',flexDirection:'column',gap:'0.5rem',maxWidth:'400px'}}><select value={shape}onChange={e=>{setShape(e.target.value);setA('');setB('');setResult('')}}style={{padding:'0.5rem'}}><option value='rectangle'>Rectangle</option><option value='triangle'>Triangle (base x height)</option><option value='circle'>Circle (radius)</option><option value='trapezoid'>Trapezoid</option></select><input value={a}onChange={e=>setA(e.target.value)}placeholder={shape==='circle'?'Radius':'Width / Base 1'} style={{padding:'0.5rem'}}/>{shape!=='circle'&&<input value={b}onChange={e=>setB(e.target.value)}placeholder={shape==='trapezoid'?'Base 2 (then height)':'Height'} style={{padding:'0.5rem'}}/>}<button onClick={calc}style={{padding:'0.5rem',background:'#3b82f6',color:'white',border:'none',cursor:'pointer'}}>Calculate</button>{result&&<div style={{background:'#1e293b',color:'white',padding:'1rem',borderRadius:'4px'}}>Area: <strong>{result}</strong></div>}</div></div>);}