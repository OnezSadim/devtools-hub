'use client';import{useState}from 'react';export default function Page(){const[num,setNum]=useState('1');const[den,setDen]=useState('1 2 1');const[r,setR]=useState('');function calc(){const n=num.trim().split(' ').filter(Boolean).map(Number);const d=den.trim().split(' ').filter(Boolean).map(Number);if(d.length===0){setR('Enter denominator coefficients.');return;}const order=d.length-1;const gain=(n[0]/d[0]).toFixed(4);setR('System order: '+order+' | DC gain: '+gain+' | Poles: '+d.length+' | Zeros: '+n.length);}return(<div style={{padding:'2rem',fontFamily:'monospace',background:'#0a0a0a',minHeight:'100vh',color:'#e0e0e0'}}><h1 style={{color:'#7c3aed'}}>Transfer Function Calculator</h1><p>Analyze H(s) = N(s)/D(s) — enter polynomial coefficients.</p><label>Numerator coefficients (space-separated, highest power first):</label><input value={num} onChange={e=>setNum(e.target.value)} style={{display:'block',margin:'8px 0',padding:'8px',background:'#1a1a2e',border:'1px solid #333',color:'#e0e0e0',width:'320px'}}/><label>Denominator coefficients (space-separated, highest power first):</label><input value={den} onChange={e=>setDen(e.target.value)} style={{display:'block',margin:'8px 0',padding:'8px',background:'#1a1a2e',border:'1px solid #333',color:'#e0e0e0',width:'320px'}}/><button onClick={calc} style={{background:'#7c3aed',color:'white',border:'none',padding:'10px 20px',cursor:'pointer'}}>Analyze</button>{r&&<p style={{marginTop:'1rem',color:'#4ade80'}}>{r}</p>}<p style={{marginTop:'1rem',color:'#888'}}>Example: H(s)=1/(s^2+2s+1) → Num: 1, Den: 1 2 1</p></div>);}