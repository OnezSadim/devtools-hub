'use client';import{useState}from 'react';export default function P(){const[d,setD]=useState('');const[r,setR]=useState('');function calc(){if(!d)return;const b=new Date(d);const n=new Date();let y=n.getFullYear()-b.getFullYear();let m=n.getMonth()-b.getMonth();if(m<0){y--;m+=12;}setR(y+' years, '+m+' months');}return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>Age Calculator</h1><p>Birthdate:<input type='date' value={d} onChange={e=>setD(e.target.value)} style={{marginLeft:'0.5rem'}}/></p><button onClick={calc}>Calculate</button>{r&&<p>Age: {r}</p>}</div>);}