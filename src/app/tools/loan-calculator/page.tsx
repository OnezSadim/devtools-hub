'use client';import{useState}from 'react';export default function P(){const[principal,setPrincipal]=useState('');const[rate,setRate]=useState('');const[years,setYears]=useState('');const[r,setR]=useState('');function calc(){const p=parseFloat(principal);const rt=parseFloat(rate)/100/12;const n=parseFloat(years)*12;if(!isNaN(p)&&!isNaN(rt)&&!isNaN(n)&&rt>0){const m=p*rt*Math.pow(1+rt,n)/(Math.pow(1+rt,n)-1);setR(m.toFixed(2));}}return(<div style={{padding:'2rem',fontFamily:'monospace',maxWidth:'500px',margin:'0 auto'}}><h1>Loan Calculator</h1><input placeholder='Loan Amount ($)' value={principal} onChange={e=>setPrincipal(e.target.value)} style={{width:'100%',padding:'8px',marginBottom:'8px',display:'block'}}/><input placeholder='Annual Interest Rate (%)' value={rate} onChange={e=>setRate(e.target.value)} style={{width:'100%',padding:'8px',marginBottom:'8px',display:'block'}}/><input placeholder='Loan Term (years)' value={years} onChange={e=>setYears(e.target.value)} style={{width:'100%',padding:'8px',marginBottom:'8px',display:'block'}}/><button onClick={calc} style={{padding:'8px 16px',background:'#0070f3',color:'#fff',border:'none',cursor:'pointer'}}>Calculate</button>{r&&<div style={{marginTop:'1rem',padding:'1rem',background:'#f0f0f0'}}><b>Monthly Payment: ${r}</b></div>}</div>);}