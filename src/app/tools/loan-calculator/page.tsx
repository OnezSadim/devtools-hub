'use client';import{useState}from 'react';export default function Page(){const[principal,setPrincipal]=useState('');const[rate,setRate]=useState('');const[years,setYears]=useState('');const[result,setResult]=useState(null);const calc=()=>{const p=parseFloat(principal),r=parseFloat(rate)/100/12,n=parseFloat(years)*12;if(!p||!r||!n)return;const monthly=p*r*Math.pow(1+r,n)/(Math.pow(1+r,n)-1);setResult({monthly:monthly.toFixed(2),total:(monthly*n).toFixed(2),interest:(monthly*n-p).toFixed(2)})};return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>Loan Calculator</h1><div style={{display:'flex',flexDirection:'column',gap:'0.5rem',maxWidth:'400px'}}><input value={principal}onChange={e=>setPrincipal(e.target.value)}placeholder='Loan amount ($)' style={{padding:'0.5rem'}}/><input value={rate}onChange={e=>setRate(e.target.value)}placeholder='Annual interest rate (%)' style={{padding:'0.5rem'}}/><input value={years}onChange={e=>setYears(e.target.value)}placeholder='Loan term (years)' style={{padding:'0.5rem'}}/><button onClick={calc}style={{padding:'0.5rem',background:'#3b82f6',color:'white',border:'none',cursor:'pointer'}}>Calculate</button>{result&&(<div style={{background:'#1e293b',color:'white',padding:'1rem',borderRadius:'4px'}}><div>Monthly Payment: <strong>${result.monthly}</strong></div><div>Total Payment: <strong>${result.total}</strong></div><div>Total Interest: <strong>${result.interest}</strong></div></div>)}</div></div>);}