'use client';import{useState}from 'react';export default function P(){const[p,setP]=useState('100000');const[r,setR]=useState('5');const[m,setM]=useState('360');const mr=parseFloat(r)/100/12;const n=parseFloat(m);const pay=parseFloat(p)*mr*Math.pow(1+mr,n)/(Math.pow(1+mr,n)-1);const total=pay*n;return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>Loan Amortization Calculator</h1><p>Principal ($):<input value={p} onChange={e=>setP(e.target.value)} type='number'/></p><p>Annual Rate (%):<input value={r} onChange={e=>setR(e.target.value)} type='number'/></p><p>Months:<input value={m} onChange={e=>setM(e.target.value)} type='number'/></p><p><b>Monthly Payment: ${isNaN(pay)?'0.00':pay.toFixed(2)}</b></p><p>Total Paid: ${isNaN(total)?'0.00':total.toFixed(2)}</p><p>Total Interest: ${isNaN(total)?'0.00':(total-parseFloat(p)).toFixed(2)}</p></div>);}