'use client';import{useState}from 'react';export default function T(){const[sig,setSig]=useState('');const[noise,setNoise]=useState('');const[res,setRes]=useState('');function calc(){const S=parseFloat(sig),N=parseFloat(noise);if(!S||!N||N===0)return;const snr=S/N;const snrDb=10*Math.log10(snr);setRes('SNR='+snr.toFixed(4)+' | SNR='+snrDb.toFixed(3)+'dB | NF='+(-snrDb).toFixed(3)+'dB');}return(<div style={{padding:'2rem',fontFamily:'monospace',maxWidth:'600px',margin:'0 auto'}}><h1>Signal-to-Noise Ratio Calculator</h1><p>Calculate SNR from signal and noise power levels.</p><div><label>Signal Power: <input type='number' value={sig} onChange={e=>setSig(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><div><label>Noise Power: <input type='number' value={noise} onChange={e=>setNoise(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><button onClick={calc} style={{padding:'0.5rem 1rem',margin:'0.5rem 0',cursor:'pointer'}}>Calculate</button>{res&&<pre style={{background:'#f5f5f5',padding:'1rem'}}>{res}</pre>}</div>);}