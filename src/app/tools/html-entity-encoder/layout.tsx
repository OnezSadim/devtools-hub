import type { Metadata } from "next";
export const metadata: Metadata = { title: "HTML Entity Encoder | DevTools Hub", description: "Encode and decode HTML entities. Convert special characters to HTML entities and back." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }