'use client';import{useState}from 'react';export default function P(){const[h,setH]=useState('');const[w,setW]=useState('');const[r,setR]=useState('');function calc(){const hm=parseFloat(h)/100;const wk=parseFloat(w);if(hm>0&&wk>0){const b=(wk/(hm*hm)).toFixed(1);setR(b+' ('+(parseFloat(b)<18.5?'Underweight':parseFloat(b)<25?'Normal':parseFloat(b)<30?'Overweight':'Obese')+')');}}return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>BMI Calculator</h1><p>Height(cm):<input value={h} onChange={e=>setH(e.target.value)} style={{marginLeft:'0.5rem'}}/></p><p>Weight(kg):<input value={w} onChange={e=>setW(e.target.value)} style={{marginLeft:'0.5rem'}}/></p><button onClick={calc}>Calculate</button>{r&&<p>Result: {r}</p>}</div>);}