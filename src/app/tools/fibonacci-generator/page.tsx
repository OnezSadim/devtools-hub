'use client';import{useState}from 'react';export default function Page(){const[n,setN]=useState('');const[seq,setSeq]=useState([]);function generate(){const count=parseInt(n);if(isNaN(count)||count<1||count>50){setSeq([]);return;}const arr=[0,1];for(let i=2;i<count;i++)arr.push(arr[i-1]+arr[i-2]);setSeq(arr.slice(0,count));}return(<div style={{padding:'2rem',fontFamily:'monospace',background:'#0f172a',minHeight:'100vh',color:'#e2e8f0'}}><h1 style={{fontSize:'1.5rem',marginBottom:'1rem'}}>Fibonacci Generator</h1><input value={n} onChange={e=>setN(e.target.value)} placeholder='How many terms? (max 50)' style={{width:'100%',padding:'0.5rem',marginBottom:'1rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px'}}/><button onClick={generate} style={{padding:'0.5rem 1rem',background:'#3b82f6',color:'white',border:'none',borderRadius:'4px',cursor:'pointer'}}>Generate</button>{seq.length>0&&<div style={{marginTop:'1rem',padding:'1rem',background:'#1e293b',borderRadius:'4px',wordBreak:'break-all'}}>{seq.join(', ')}</div>}</div>);}