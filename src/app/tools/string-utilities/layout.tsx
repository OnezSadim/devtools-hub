import type { Metadata } from "next";
export const metadata: Metadata = { title: "String Utilities | DevTools Hub", description: "Reverse strings, count characters, remove whitespace, truncate, pad, and more string operations." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }