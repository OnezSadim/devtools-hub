'use client';import{useState}from 'react';export default function T(){const[r,setR]=useState('');const[c,setC]=useState('');const[res,setRes]=useState('');function calc(){const R=parseFloat(r),C=parseFloat(c)*1e-6;if(!R||!C)return;const tau=R*C;const fc=1/(2*Math.PI*R*C);setRes('tau='+tau.toExponential(3)+'s | fc='+fc.toFixed(2)+'Hz');}return(<div style={{padding:'2rem',fontFamily:'monospace',maxWidth:'600px',margin:'0 auto'}}><h1>RC Circuit Calculator</h1><p>Time constant and cutoff frequency for RC circuits.</p><div><label>R (Ohm): <input type='number' value={r} onChange={e=>setR(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><div><label>C (uF): <input type='number' value={c} onChange={e=>setC(e.target.value)} style={{margin:'0.25rem',padding:'0.25rem'}}/></label></div><button onClick={calc} style={{padding:'0.5rem 1rem',margin:'0.5rem 0',cursor:'pointer'}}>Calculate</button>{res&&<pre style={{background:'#f5f5f5',padding:'1rem'}}>{res}</pre>}</div>);}