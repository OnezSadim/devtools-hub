"use client";import{{useState}}from"react";export default function Page(){{const[v,setV]=useState("");const[f,setF]=useState("1");const convert=(val,factor)=>{{if(!val)return"";const n=parseFloat(val);if(isNaN(n))return"Invalid";return(n/parseFloat(factor)).toFixed(6)}};return(<div style={{{{padding:"2rem",maxWidth:"600px",margin:"0 auto"}}}}>  <h1>Luminous Intensity Converter</h1><p>Convert between candela, millicandela, kilocandela</p><input type="number" value={{v}} onChange={{e=>setV(e.target.value)}} placeholder="Enter value" style={{{{width:"100%",padding:"0.5rem",marginBottom:"1rem"}}}}/><select value={{f}} onChange={{e=>setF(e.target.value)}} style={{{{padding:"0.5rem",marginBottom:"1rem"}}}}><option value="1">cd</option><option value="1000">mcd</option><option value="0.001">kcd</option><option value="0.981">HK</option></select><div>{{convert(v,f)}}</div></div>)}}
