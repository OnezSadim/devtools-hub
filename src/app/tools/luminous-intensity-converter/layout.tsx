import { Metadata } from "next";
export const metadata: Metadata = { title: "Luminous Intensity Converter", description: "Convert between luminous intensity units: candela, millicandela, kilocandela." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
