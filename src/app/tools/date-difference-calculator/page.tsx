'use client';import{useState}from 'react';export default function P(){const[d1,setD1]=useState('');const[d2,setD2]=useState('');const[r,setR]=useState('');function calc(){if(!d1||!d2)return;const a=new Date(d1);const b=new Date(d2);const diff=Math.abs(b-a);const days=Math.floor(diff/(1000*60*60*24));const weeks=Math.floor(days/7);setR(days+' days ('+weeks+' weeks)');}return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>Date Difference Calculator</h1><p>Date 1:<input type='date' value={d1} onChange={e=>setD1(e.target.value)} style={{marginLeft:'0.5rem'}}/></p><p>Date 2:<input type='date' value={d2} onChange={e=>setD2(e.target.value)} style={{marginLeft:'0.5rem'}}/></p><button onClick={calc}>Calculate</button>{r&&<p>Difference: {r}</p>}</div>);}