import { Metadata } from "next";
export const metadata: Metadata = { title: "Angular Velocity Converter | DevTools Hub", description: "Convert between angular velocity units like rad/s, deg/s, RPM, RPS." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>;  }
