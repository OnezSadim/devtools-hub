"use client";import{useState}from"react";export default function Page(){const[val,setVal]=useState("");const[from,setFrom]=useState(""+"radian_per_second"+"");const[to,setTo]=useState(""+"degree_per_second"+"");function convert(){if(!val||isNaN(Number(val)))return"Invalid";let v=parseFloat(val),base=0;switch(from){case "radian_per_second": base = v / 1; break;case "degree_per_second": base = v / 0.0174533; break;case "revolution_per_minute": base = v / 0.10472; break;case "revolution_per_second": base = v / 6.28318; break;case "radian_per_minute": base = v / 0.0166667; break;}let result=0;switch(to){case "radian_per_second": result = base * 1; break;case "degree_per_second": result = base * 0.0174533; break;case "revolution_per_minute": result = base * 0.10472; break;case "revolution_per_second": result = base * 6.28318; break;case "radian_per_minute": result = base * 0.0166667; break;}return result.toPrecision(6);}return(<div style={{padding:"2rem",fontFamily:"sans-serif",maxWidth:600,margin:"0 auto"}}><h1>Angular Velocity Converter</h1><p>Convert between radians per second, degrees per second, RPM, and more</p><input type="number" value={val} onChange={e=>setVal(e.target.value)} placeholder="Value" style={{padding:"0.5rem",marginRight:"0.5rem",width:120}}/><select value={from} onChange={e=>setFrom(e.target.value)} style={{padding:"0.5rem",marginRight:"0.5rem"}}><option value="radian_per_second">radian per second</option><option value="degree_per_second">degree per second</option><option value="revolution_per_minute">revolution per minute</option><option value="revolution_per_second">revolution per second</option><option value="radian_per_minute">radian per minute</option></select><span>→</span><select value={to} onChange={e=>setTo(e.target.value)} style={{padding:"0.5rem",marginLeft:"0.5rem"}}><option value="radian_per_second">radian per second</option><option value="degree_per_second">degree per second</option><option value="revolution_per_minute">revolution per minute</option><option value="revolution_per_second">revolution per second</option><option value="radian_per_minute">radian per minute</option></select><div style={{marginTop:"1rem",fontSize:"1.5rem"}}>{val&&<span>{val} {from.replace(/_/g," ")} = <strong>{convert()}</strong> {to.replace(/_/g," ")}</span>}</div></div>);}