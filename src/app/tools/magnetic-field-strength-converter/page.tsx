"use client";import{useState}from"react";const units=[{name:"Ampere/meter",factor:1},{name:"Oersted",factor:79.5775},{name:"Kiloampere/meter",factor:1000.0},{name:"Milliampere/meter",factor:0.001}];export default function Page(){const[val,setVal]=useState("");const[from,setFrom]=useState(0);const sel=(i)=>units[i];const conv=(t)=>{const n=parseFloat(val);if(isNaN(n))return"";return((n*sel(from).factor)/sel(t).factor).toPrecision(6);};return(<div style={{padding:"2rem",fontFamily:"sans-serif",maxWidth:"600px",margin:"0 auto"}}><h1>Magnetic Field Strength Converter</h1><p>Convert between ampere per meter, oersted, and more magnetic field strength units.</p><div style={{display:"flex",gap:"1rem",marginBottom:"1rem"}}><input type="number" value={val} onChange={e=>setVal(e.target.value)} placeholder="Enter value" style={{flex:1,padding:"0.5rem",fontSize:"1rem"}}/><select value={from} onChange={e=>setFrom(Number(e.target.value))} style={{padding:"0.5rem"}}>{units.map((u,i)=><option key={i} value={i}>{u.name}</option>)}</select></div><table style={{width:"100%",borderCollapse:"collapse"}}><tbody>{units.map((u,i)=><tr key={i} style={{borderBottom:"1px solid #eee"}}><td style={{padding:"0.5rem"}}>{u.name}</td><td style={{padding:"0.5rem",fontWeight:"bold"}}>{conv(i)||"-"}</td></tr>)}</tbody></table></div>);}
