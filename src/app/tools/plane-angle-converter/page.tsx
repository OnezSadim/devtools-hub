"use client";import{{useState}}from"react";export default function Page(){{const[v,setV]=useState("");const[f,setF]=useState("1");const convert=(val,factor)=>{{if(!val)return"";const n=parseFloat(val);if(isNaN(n))return"Invalid";return(n/parseFloat(factor)).toFixed(6)}};return(<div style={{{{padding:"2rem",maxWidth:"600px",margin:"0 auto"}}}}>  <h1>Plane Angle Converter</h1><p>Convert between degree, radian, gradian, arcminute, arcsecond</p><input type="number" value={{v}} onChange={{e=>setV(e.target.value)}} placeholder="Enter value" style={{{{width:"100%",padding:"0.5rem",marginBottom:"1rem"}}}}/><select value={{f}} onChange={{e=>setF(e.target.value)}} style={{{{padding:"0.5rem",marginBottom:"1rem"}}}}><option value="1">deg</option><option value="0.017453">rad</option><option value="1.11111">grad</option><option value="60">arcmin</option><option value="3600">arcsec</option></select><div>{{convert(v,f)}}</div></div>)}}
