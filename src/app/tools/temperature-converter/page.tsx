'use client';import{useState}from 'react';export default function TemperatureConverter(){const[val,setVal]=useState('');const[from,setFrom]=useState('celsius');const[to,setTo]=useState('fahrenheit');const[result,setResult]=useState('');const units=['celsius','fahrenheit','kelvin'];function toK(v,u){if(u==='celsius')return v+273.15;if(u==='fahrenheit')return(v-32)*5/9+273.15;return v;}function fromK(k,u){if(u==='celsius')return k-273.15;if(u==='fahrenheit')return(k-273.15)*9/5+32;return k;}function convert(){const v=parseFloat(val);if(isNaN(v)){setResult('Enter a number');return;}setResult(fromK(toK(v,from),to).toFixed(4)+' '+to);}return(<div style={{padding:'2rem',fontFamily:'monospace',background:'#111',minHeight:'100vh',color:'#eee'}}><h1 style={{color:'#7c3aed'}}>Temperature Converter</h1><input value={val} onChange={e=>setVal(e.target.value)} placeholder='Value' style={{padding:'0.5rem',marginRight:'0.5rem',background:'#222',color:'#eee',border:'1px solid #444'}}/><select value={from} onChange={e=>setFrom(e.target.value)} style={{padding:'0.5rem',marginRight:'0.5rem',background:'#222',color:'#eee',border:'1px solid #444'}}>{units.map(u=><option key={u} value={u}>{u}</option>)}</select><span style={{marginRight:'0.5rem'}}>to</span><select value={to} onChange={e=>setTo(e.target.value)} style={{padding:'0.5rem',marginRight:'0.5rem',background:'#222',color:'#eee',border:'1px solid #444'}}>{units.map(u=><option key={u} value={u}>{u}</option>)}</select><button onClick={convert} style={{padding:'0.5rem 1rem',background:'#7c3aed',color:'#fff',border:'none',cursor:'pointer'}}>Convert</button>{result&&<p style={{marginTop:'1rem',color:'#a78bfa'}}>Result: {result}</p>}</div>);}
