'use client';import { useState } from 'react';export default function TempConverter() { const [val, setVal] = useState(''); const [from, setFrom] = useState('celsius'); const units = ['celsius','fahrenheit','kelvin']; function convert(v, f) { const n = parseFloat(v); if (isNaN(n)) return {}; let c = f === 'celsius' ? n : f === 'fahrenheit' ? (n-32)*5/9 : n-273.15; return { celsius: c.toFixed(4), fahrenheit: (c*9/5+32).toFixed(4), kelvin: (c+273.15).toFixed(4) }; } const res = convert(val, from); return (<div style={{padding:'2rem',maxWidth:'500px',margin:'0 auto',fontFamily:'monospace'}}><h1 style={{color:'#7dd3fc'}}>Temperature Converter</h1><input value={val} onChange={e=>setVal(e.target.value)} placeholder='Enter value' style={{width:'100%',padding:'0.5rem',marginBottom:'0.5rem',background:'#1e293b',color:'#fff',border:'1px solid #334155',borderRadius:'4px'}} /><select value={from} onChange={e=>setFrom(e.target.value)} style={{width:'100%',padding:'0.5rem',marginBottom:'1rem',background:'#1e293b',color:'#fff',border:'1px solid #334155',borderRadius:'4px'}}>{units.map(u=><option key={u} value={u}>{u}</option>)}</select>{val && Object.keys(res).length > 0 && <div>{units.map(u=><div key={u} style={{padding:'0.5rem',marginBottom:'0.5rem',background:'#1e293b',borderRadius:'4px'}}><strong style={{color:'#94a3b8'}}>{u}:</strong> <span style={{color:'#7dd3fc'}}>{res[u]}</span></div>)}</div>}</div>); }