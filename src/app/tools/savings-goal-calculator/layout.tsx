export const metadata = {
  title: 'Savings Goal Calculator | DevTools Hub',
  description: 'Calculate how long to reach your savings goal with monthly contributions.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
