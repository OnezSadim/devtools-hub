import { Metadata } from "next";
export const metadata: Metadata = { title: "Torque Converter | DevTools Hub", description: "Convert between torque units like N·m, ft·lb, in·lb, kgf·m." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>;  }
