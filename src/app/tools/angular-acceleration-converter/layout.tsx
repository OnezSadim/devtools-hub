import { Metadata } from "next";
export const metadata: Metadata = { title: "Angular Acceleration Converter | DevTools Hub", description: "Convert between angular acceleration units like rad/s2, deg/s2, rev/s2." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>;  }
