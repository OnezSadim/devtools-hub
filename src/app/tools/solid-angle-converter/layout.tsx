import { Metadata } from "next";
export const metadata: Metadata = { title: "Solid Angle Converter", description: "Convert between solid angle units: steradian, millisteradian, square degree." };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
