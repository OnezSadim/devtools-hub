'use client';import{useState}from 'react';export default function P(){const[p,setP]=useState('');const[r,setR]=useState('');const[n,setN]=useState('12');const[t,setT]=useState('');const[res,setRes]=useState('');function calc(){const pr=parseFloat(p);const rt=parseFloat(r)/100;const nt=parseFloat(n);const yr=parseFloat(t);if([pr,rt,nt,yr].every(x=>!isNaN(x))){const a=pr*Math.pow(1+rt/nt,nt*yr);setRes(a.toFixed(2));}}return(<div style={{padding:'2rem',fontFamily:'monospace',maxWidth:'500px',margin:'0 auto'}}><h1>Compound Interest Calculator</h1><input placeholder='Principal ($)' value={p} onChange={e=>setP(e.target.value)} style={{width:'100%',padding:'8px',marginBottom:'8px',display:'block'}}/><input placeholder='Annual Rate (%)' value={r} onChange={e=>setR(e.target.value)} style={{width:'100%',padding:'8px',marginBottom:'8px',display:'block'}}/><input placeholder='Compounds per Year' value={n} onChange={e=>setN(e.target.value)} style={{width:'100%',padding:'8px',marginBottom:'8px',display:'block'}}/><input placeholder='Years' value={t} onChange={e=>setT(e.target.value)} style={{width:'100%',padding:'8px',marginBottom:'8px',display:'block'}}/><button onClick={calc} style={{padding:'8px 16px',background:'#0070f3',color:'#fff',border:'none',cursor:'pointer'}}>Calculate</button>{res&&<div style={{marginTop:'1rem',padding:'1rem',background:'#f0f0f0'}}><b>Final Amount: ${res}</b><br/>Interest Earned: ${(parseFloat(res)-parseFloat(p)).toFixed(2)}</div>}</div>);}