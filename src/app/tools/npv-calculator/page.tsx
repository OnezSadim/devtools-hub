'use client';import{useState}from 'react';export default function P(){const[rate,setRate]=useState('10');const[flows,setFlows]=useState('-1000,300,400,500,200');const cf=flows.split(',').map(Number);const r=parseFloat(rate)/100;const npv=cf.reduce((acc,v,i)=>acc+v/Math.pow(1+r,i),0);return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>NPV Calculator</h1><p>Discount Rate (%):<input value={rate} onChange={e=>setRate(e.target.value)} type='number'/></p><p>Cash Flows (comma-separated):<br/><input value={flows} onChange={e=>setFlows(e.target.value)} style={{width:'320px'}}/></p><p><b>NPV: ${isNaN(npv)?'0.00':npv.toFixed(2)}</b></p><p>{npv>0?'Positive NPV - worthwhile investment':'Negative NPV - review investment'}</p></div>);}