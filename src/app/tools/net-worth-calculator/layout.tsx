export const metadata = {
  title: 'Net Worth Calculator | DevTools Hub',
  description: 'Calculate your net worth by subtracting liabilities from assets.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
