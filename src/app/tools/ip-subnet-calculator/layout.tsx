import type { Metadata } from "next";
export const metadata: Metadata = { title: "IP Subnet Calculator | DevTools Hub", description: "Calculate IP subnet information: network address, broadcast, host range, and number of hosts." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }