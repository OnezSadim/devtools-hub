"use client";import{useState}from"react";const units=[{"name":"Farad","factor":1},{"name":"Millifarad","factor":0.001},{"name":"Microfarad","factor":1e-06},{"name":"Nanofarad","factor":1e-09},{"name":"Picofarad","factor":1e-12}];export default function Page(){const[val,setVal]=useState("");const[from,setFrom]=useState(0);const convert=(u)=>{if(!val||isNaN(Number(val)))return"";return((Number(val)/units[from].factor)*u.factor).toPrecision(6);};return(<div style={{padding:"2rem",maxWidth:800,margin:"0 auto"}}><h1>Capacitance Converter</h1><p>Convert between capacitance units</p><input type="number" value={val} onChange={e=>setVal(e.target.value)} style={{padding:"0.5rem",marginBottom:"1rem",width:"100%"}}/><select value={from} onChange={e=>setFrom(Number(e.target.value))} style={{padding:"0.5rem",marginBottom:"1rem"}}>{units.map((u,i)=><option key={i} value={i}>{u.name}</option>)}</select><table style={{width:"100%",borderCollapse:"collapse"}}><tbody>{units.map((u,i)=><tr key={i} style={{borderBottom:"1px solid #333"}}><td style={{padding:"0.5rem"}}>{u.name}</td><td style={{padding:"0.5rem",fontWeight:"bold"}}>{convert(u)||"-"}</td></tr>)}</tbody></table></div>);}