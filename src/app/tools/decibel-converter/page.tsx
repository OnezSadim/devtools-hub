'use client';import{useState}from 'react';export default function Page(){const[val,setVal]=useState('1');const[mode,setMode]=useState('ratio_to_db');const convert=()=>{const v=parseFloat(val)||0;if(mode==='ratio_to_db')return(20*Math.log10(v)).toFixed(4)+' dB';if(mode==='db_to_ratio')return Math.pow(10,v/20).toFixed(6);if(mode==='power_to_db')return(10*Math.log10(v)).toFixed(4)+' dB';return Math.pow(10,v/10).toFixed(6);};return(<div style={{padding:'2rem',fontFamily:'monospace',background:'#0f172a',minHeight:'100vh',color:'#e2e8f0'}}><h1 style={{color:'#38bdf8'}}>Decibel Converter</h1><div><label>Value: <input value={val} onChange={e=>setVal(e.target.value)} style={{background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',padding:'4px',marginLeft:'8px'}}/></label></div><div style={{marginTop:'0.5rem'}}><select value={mode} onChange={e=>setMode(e.target.value)} style={{background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',padding:'4px'}}><option value='ratio_to_db'>Voltage ratio → dB</option><option value='db_to_ratio'>dB → Voltage ratio</option><option value='power_to_db'>Power ratio → dB</option><option value='db_to_power'>dB → Power ratio</option></select></div><div style={{marginTop:'1rem',padding:'1rem',background:'#1e293b',borderRadius:'8px'}}><strong>Result:</strong> {convert()}</div></div>);};