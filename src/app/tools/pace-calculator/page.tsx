'use client';import{useState}from 'react';export default function P(){const[dist,setDist]=useState('');const[mins,setMins]=useState('');const[secs,setSecs]=useState('');const[r,setR]=useState('');function calc(){const d=parseFloat(dist);const t=parseInt(mins)*60+parseInt(secs||'0');if(d>0&&t>0){const pace=t/d;const pm=Math.floor(pace/60);const ps=Math.round(pace%60).toString().padStart(2,'0');setR(pm+':'+ps+' /km');}}return(<div style={{padding:'2rem',fontFamily:'monospace'}}><h1>Pace Calculator</h1><p>Distance(km):<input value={dist} onChange={e=>setDist(e.target.value)} style={{marginLeft:'0.5rem',width:'60px'}}/></p><p>Minutes:<input value={mins} onChange={e=>setMins(e.target.value)} style={{marginLeft:'0.5rem',width:'60px'}}/></p><p>Seconds:<input value={secs} onChange={e=>setSecs(e.target.value)} style={{marginLeft:'0.5rem',width:'60px'}}/></p><button onClick={calc}>Calculate</button>{r&&<p>Pace: {r}</p>}</div>);}