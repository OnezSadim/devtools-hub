'use client';import{useState}from 'react';export default function InflationCalculator(){const[amount,setAmount]=useState('1000');const[rate,setRate]=useState('3');const[years,setYears]=useState('10');const[result,setResult]=useState(null);function calc(){const a=parseFloat(amount);const r=parseFloat(rate)/100;const n=parseFloat(years);const future=a*Math.pow(1+r,n);const loss=future-a;setResult({future:future.toFixed(2),loss:loss.toFixed(2),purchasing:(a/future*100).toFixed(1)});}return(<div style={{fontFamily:'monospace',background:'#0f172a',minHeight:'100vh',padding:'2rem',color:'#e2e8f0'}}><h1 style={{fontSize:'1.5rem',marginBottom:'1rem'}}>Inflation Calculator</h1><div style={{display:'grid',gap:'1rem',maxWidth:'400px'}}><div><label>Current Amount ($)</label><input value={amount} onChange={e=>setAmount(e.target.value)} style={{width:'100%',padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginTop:'0.25rem'}} /></div><div><label>Inflation Rate (%/year)</label><input value={rate} onChange={e=>setRate(e.target.value)} style={{width:'100%',padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginTop:'0.25rem'}} /></div><div><label>Years</label><input value={years} onChange={e=>setYears(e.target.value)} style={{width:'100%',padding:'0.5rem',background:'#1e293b',color:'#e2e8f0',border:'1px solid #334155',borderRadius:'4px',marginTop:'0.25rem'}} /></div><button onClick={calc} style={{padding:'0.75rem',background:'#3b82f6',color:'white',border:'none',borderRadius:'4px',cursor:'pointer'}}>Calculate</button>{result&&<div style={{background:'#1e293b',padding:'1rem',borderRadius:'4px'}}><p>Future Value Needed: <strong>${result.future}</strong></p><p>Purchasing Power Lost: <strong>${result.loss}</strong></p><p>Purchasing Power Remaining: <strong>{result.purchasing}%</strong></p></div>}</div></div>);}
