import type { Metadata } from "next";
export const metadata: Metadata = { title: "JSON to CSV Converter | DevTools Hub", description: "Convert JSON arrays to CSV format online. Download or copy the converted CSV data instantly." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }