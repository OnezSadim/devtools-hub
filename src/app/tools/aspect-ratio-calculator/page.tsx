'use client';import { useState } from 'react';export default function AspectRatioCalculator() { const [w, setW] = useState(''); const [h, setH] = useState(''); const [result, setResult] = useState(null); const gcd = (a, b) => b === 0 ? a : gcd(b, a % b); const calc = () => { const width = parseFloat(w), height = parseFloat(h); if (!width || !height) return; const g = gcd(Math.round(width), Math.round(height)); const rw = Math.round(width) / g, rh = Math.round(height) / g; const dec = width / height; setResult({ ratio: rw + ':' + rh, decimal: dec.toFixed(4), w43: h ? (parseFloat(h) * 4 / 3).toFixed(0) : '', h43: w ? (parseFloat(w) * 3 / 4).toFixed(0) : '', w169: h ? (parseFloat(h) * 16 / 9).toFixed(0) : '', h169: w ? (parseFloat(w) * 9 / 16).toFixed(0) : '' }); }; return (<div style={{padding:'2rem',maxWidth:'500px',margin:'0 auto',fontFamily:'monospace'}}><h1 style={{color:'#a78bfa'}}>Aspect Ratio Calculator</h1><div style={{display:'flex',gap:'1rem',marginBottom:'0.75rem'}}><div style={{flex:1}}><label style={{color:'#94a3b8',display:'block',marginBottom:'0.25rem'}}>Width</label><input value={w} onChange={e => setW(e.target.value)} type='number' placeholder='e.g. 1920' style={{width:'100%',padding:'0.75rem',background:'#1e1b4b',color:'#e2e8f0',border:'1px solid #4c1d95',borderRadius:'6px',boxSizing:'border-box'}} /></div><div style={{flex:1}}><label style={{color:'#94a3b8',display:'block',marginBottom:'0.25rem'}}>Height</label><input value={h} onChange={e => setH(e.target.value)} type='number' placeholder='e.g. 1080' style={{width:'100%',padding:'0.75rem',background:'#1e1b4b',color:'#e2e8f0',border:'1px solid #4c1d95',borderRadius:'6px',boxSizing:'border-box'}} /></div></div><button onClick={calc} style={{width:'100%',padding:'0.75rem',background:'#7c3aed',color:'#fff',border:'none',borderRadius:'6px',cursor:'pointer',marginBottom:'1rem'}}>Calculate</button>{result && <div style={{background:'#1e1b4b',borderRadius:'6px',padding:'1rem'}}><p style={{color:'#a78bfa',fontSize:'1.5rem',textAlign:'center',margin:'0 0 1rem 0'}}>{result.ratio}</p><p style={{color:'#94a3b8',margin:'0.25rem 0'}}>Decimal: {result.decimal}</p><p style={{color:'#94a3b8',margin:'0.25rem 0'}}>4:3 equivalent: {result.w43} x {h} or {w} x {result.h43}</p><p style={{color:'#94a3b8',margin:'0.25rem 0'}}>16:9 equivalent: {result.w169} x {h} or {w} x {result.h169}</p></div>}</div>); }