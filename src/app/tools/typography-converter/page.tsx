'use client';import { useState } from 'react';export default function TypographyConverter() { const [val, setVal] = useState('16'); const [from, setFrom] = useState('px'); const [base, setBase] = useState('16'); const toPx = {'px':1,'pt':1.333,'em':parseFloat(base)||16,'rem':parseFloat(base)||16,'%':(parseFloat(base)||16)/100,'vw':window?.innerWidth/100||10}; const units = ['px','pt','em','rem','%']; const convert = (target) => { const px = parseFloat(val) * (typeof toPx[from]==='number'?toPx[from]:16); const d = typeof toPx[target]==='number'?toPx[target]:16; return (px/d).toFixed(4); }; return (<div style={{padding:'2rem',fontFamily:'monospace',background:'#1a1a2e',minHeight:'100vh',color:'#e0e0e0'}}><h1 style={{color:'#00d4ff'}}>Typography Unit Converter</h1><p>Convert between px, pt, em, rem, % units.</p><div style={{display:'flex',gap:'1rem',flexWrap:'wrap',marginTop:'1rem',alignItems:'center'}}><input value={val} onChange={e=>setVal(e.target.value)} placeholder='Value' style={{padding:'0.5rem',background:'#16213e',color:'#e0e0e0',border:'1px solid #00d4ff',borderRadius:'4px',width:'80px'}}/><select value={from} onChange={e=>setFrom(e.target.value)} style={{padding:'0.5rem',background:'#16213e',color:'#e0e0e0',border:'1px solid #00d4ff',borderRadius:'4px'}}>{units.map(u=><option key={u}>{u}</option>)}</select><span>Base:</span><input value={base} onChange={e=>setBase(e.target.value)} placeholder='16' style={{padding:'0.5rem',background:'#16213e',color:'#e0e0e0',border:'1px solid #444',borderRadius:'4px',width:'60px'}}/></div>{val && <div style={{marginTop:'1rem',padding:'1rem',background:'#16213e',borderRadius:'8px'}}>{units.filter(u=>u!==from).map(u=><div key={u} style={{fontSize:'1.1rem',color:'#00d4ff',marginBottom:'0.5rem'}}>{val} {from} = {convert(u)} {u}</div>)}</div>}</div>); }